/* header to main.c */

#include "libs.h"           /* include C standard libraries */
#include "exit_codes.h"     /* include exit codes header */

/* functions in use from first_pass.c */
int first_pass(char *); /* main sends the files to the assembler by calling first_pass() */
